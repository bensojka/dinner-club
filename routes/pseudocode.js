// in the groups_locations tables we can track how many votes each location has

// in the users_groups table we can track if the user has a vote they can use



// login on the first page

// the go to the groups page

// send a request to the database for the user to pull all the groups and with the groups pull all the locations specific to that user
// so they can see all the votes
// the data that gets sent back will be used to populate the page